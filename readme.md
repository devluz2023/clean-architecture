# RH

This is a Maven project with multiple modules.

## Prerequisites

Before running the project, ensure you have the following installed:

- Java Development Kit (JDK) 1.8 or higher
- Apache Maven

## Installation

To build the project, navigate to the root directory of the project in your terminal and run the following command:

```bash
mvn clean install
